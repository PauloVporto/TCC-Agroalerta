# Copie este arquivo para ".env" e preencha com suas chaves reais.
# Sem essas chaves, o sistema roda em MODO SIMULADO (dados fictícios),
# então dá pra testar tudo mesmo sem ter as chaves ainda.

# Chave da API de clima (OpenWeatherMap - plano gratuito funciona)
# Crie a sua em: https://openweathermap.org/api
OPENWEATHER_API_KEY=

# Chave da API de IA para o módulo de análise de mercado (Módulo 4)
# Pode ser da Anthropic (Claude) ou OpenAI - o código já vem pronto para Anthropic
ANTHROPIC_API_KEY=

# Chave do Google Maps (Geocoding API) - usada para converter o endereço
# do talhão em latitude/longitude, e exibir o mapa no frontend.
# Crie/gerencie a sua em: https://console.cloud.google.com/apis/credentials
# IMPORTANTE: restrinja essa chave por IP/domínio no Console do Google
# assim que possível, para evitar uso indevido.
GOOGLE_MAPS_API_KEY=

# Porta do servidor (opcional)
PORT=3001
