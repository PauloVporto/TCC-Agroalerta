// Copie este arquivo para "config.js" e preencha com sua chave real.
// Diferente da chave do backend, a chave do Google Maps JS API É
// destinada a rodar no navegador - mas ainda assim deve ser restrita
// por domínio/referrer no Console do Google para evitar uso indevido.
// https://console.cloud.google.com/apis/credentials

window.GOOGLE_MAPS_BROWSER_KEY = "";
